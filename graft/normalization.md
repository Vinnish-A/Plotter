# Normalization

Normalize plotting code to a stable interface:

- `data`
- `mapping`
- `focus`
- `layout`
- `style`
- `output`

Material cases use the simpler Linux build interface: `data_main.csv`, optional `data_optional.csv`, and `outputs/rebuilt.png`.
