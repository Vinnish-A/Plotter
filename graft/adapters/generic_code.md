# Generic Code Adapter

When no framework-specific adapter applies, isolate the plotting section, declare dependencies, replace local inputs with standardized CSV files, and preserve only the reusable visual grammar.
