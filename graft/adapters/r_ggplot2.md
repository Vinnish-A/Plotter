# R ggplot2 Adapter

Prefer `read.csv()` or `readr::read_csv()` for standardized CSV inputs.

Scripts must write to `outputs/rebuilt.png` and avoid package installation, absolute paths, and mutation of raw source material.
