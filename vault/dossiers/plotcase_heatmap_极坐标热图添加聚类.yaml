id: plotcase_heatmap_极坐标热图添加聚类
title: 极坐标热图添加聚类
origin:
  case_dir: vault/material/plotcase_heatmap_极坐标热图添加聚类
  source:
    root: raw/plotcase_export
    original_path: heatmap\极坐标热图添加聚类
scientific_intent: &id001
- annotated_heatmap visual grammar
- heatmap
- heatmap
- matrix_body
- annotation_tracks
intent: *id001
visual_genes:
  layout:
  - cartesian
  marks:
  - point
  encodings:
  - x
  - y
  - color
  - size
  narrative:
  - global_comparison
  emphasis:
  - focus_halo
  - callout
  - reserved_label_space
data_roles:
  required:
    row_id:
      kind: inferred
      semantic: row_id
    column_id:
      kind: inferred
      semantic: column_id
    value:
      kind: inferred
      semantic: value
  optional:
    row_group:
      kind: inferred
      use: optional_visual_module
    column_group:
      kind: inferred
      use: optional_visual_module
    annotation_track:
      kind: inferred
      use: optional_visual_module
    annotation_value:
      kind: inferred
      use: optional_visual_module
    label:
      kind: inferred
      use: optional_visual_module
    size_value:
      kind: inferred
      use: optional_visual_module
    facet:
      kind: inferred
      use: optional_visual_module
required_data:
- row_id
- column_id
- value
optional_data:
- row_group
- column_group
- annotation_track
- annotation_value
- label
- size_value
- facet
optional_modules:
  detail_panel:
    requires:
    - group
    - sample_x
    - sample_y
    can_query_environment: true
    fallback_if_missing: disable_detail_panel
  annotation_track:
    requires:
    - group
    - value
    can_query_environment: true
    fallback_if_missing: use_plain_axis
  uncertainty_interval:
    requires_any:
    - - lower
      - upper
    - - se
    - - ci_low
      - ci_high
    can_query_environment: true
    fallback_if_missing: do_not_show_uncertainty
  subgroup_color:
    requires:
    - subgroup
    can_query_environment: true
    fallback_if_missing: use_primary_group_only
  network_weight:
    requires:
    - source
    - target
    - weight
    enabled_by_default: false
    fallback_if_missing: draw_unweighted_edges
function_signature:
  name: plot
  arguments:
  - data
  - mapping
  - focus
  - layout
  - style
  - output
  entry: plot.py
supports:
  highlight: true
  composition: false
  detail_panel: false
  batch_output: true
style_compatibility:
- default_scientific
- blue_white_red_dense
complexity:
  level: medium
defamiliarization:
  level: U2
best_for:
- 'Reusable annotated_heatmap cases with roles: row_id, column_id, value'
bad_for:
- Missing required roles
- Undeclared raw resources
- Unbounded labels without focus selection
failure_cases:
- Missing output image
- Blank image
- Required role not materialized
minimal_reproducible_example:
  data: vault/material/plotcase_heatmap_极坐标热图添加聚类/data_main.csv
  entry: vault/material/plotcase_heatmap_极坐标热图添加聚类/plot.py
  preview: vault/material/plotcase_heatmap_极坐标热图添加聚类/outputs/rebuilt.png
rebuild_class:
  source_code_rebuild: true
  generic_renderer_rebuild: true
  case_level_fallback: false
  synthetic_data: true
vault_status:
  live: true
  folded: false
  restored_from_fold: true
  canonical_case: null
  reason: retained after visual folding review; complex or distinctive non-map visual
    grammar
