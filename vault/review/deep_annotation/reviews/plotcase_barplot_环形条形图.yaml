case_id: plotcase_barplot_环形条形图
annotation_version: deep_annotation_model_review_v1
annotation_status:
  status: complete
  level: model_assisted_deep_review
  human_review_required: false
  reason: Image, metadata, guide, standard script, CSV profile, machine evidence, card, schema, and prior review were inspected
    for this batch review.
annotator_model: gpt-5-codex_model_assisted_review
reviewed_at: '2026-05-21T00:00:00+08:00'
evidence:
  files_read:
  - AGENTS.md
  - schemas/deep_dossier_review.schema.yaml
  - vault/material/plotcase_barplot_环形条形图/metadata.json
  - vault/material/plotcase_barplot_环形条形图/agent_guide.md
  - vault/material/plotcase_barplot_环形条形图/plot.py
  - vault/material/plotcase_barplot_环形条形图/data_main.csv
  - vault/material/plotcase_barplot_环形条形图/outputs/rebuilt.png
  - vault/evidence/machine/plotcase_barplot_环形条形图.yaml
  - vault/cards/plotcase_barplot_环形条形图.yaml
  - vault/review/deep_annotation/reviews/plotcase_barplot_环形条形图.yaml
  data_optional: absent
  machine_evidence: vault/evidence/machine/plotcase_barplot_环形条形图.yaml
  asset_card: vault/cards/plotcase_barplot_环形条形图.yaml
  review_record: vault/review/deep_annotation/reviews/plotcase_barplot_环形条形图.yaml
  image_size:
    exists: true
    size_bytes: 412572
    width: 1663
    height: 1466
    mean_luma: 242.3255
    stddev_luma: 42.8914
    blank_like: false
  data_rows_inspected: 9
  rebuild_class:
    source_code_rebuild: true
    generic_renderer_rebuild: true
    case_level_fallback: false
    synthetic_data: false
observed_visual_grammar:
  geometry: radial bar chart
  subtype: circular grouped bar summary
  layout: single radial panel
  primary_marks: &id001
  - radial bars
  - concentric grid rings
  - outer group brackets
  - rotated labels
  visible_encodings: bar length encodes magnitude; hue groups category bands; radial position orders labels
inferred_visual_grammar:
  declared_geometry: map
  declared_subtype: map
  metadata_keywords:
  - bar
  - stacked_or_grouped
  - summary_layer
  reusable_pattern: radial category-value summaries with grouped sectors
  domain_semantics: Only visual roles were reviewed; biological or clinical meanings are not treated as authoritative unless
    visible in labels/axes.
  core_exclusion_note: Generic renderer, fallback, and synthetic-data flags prevent promotion to core retrieval.
reviewed_visual_roles:
  marks: *id001
  encodings: bar length encodes magnitude; hue groups category bands; radial position orders labels
  axes: polar radius with numeric ring ticks; angular category labels around the ring
  legends: no separate legend; color/group semantics carried by outer labels
  annotations: outer arc labels and category text around circle
canonical_columns: &id002
- category
- value
- group
- stack
- error_low
- error_high
- line_value
- annotation_value
- label
required_data_semantics:
  category: categorical bar position/order
  value: bar magnitude
optional_data_semantics:
  group: sector or color grouping when populated
  stack: secondary grouping when populated
  error_low: available interval lower value, not visible in reviewed radial output
  error_high: available interval upper value, not visible in reviewed radial output
  line_value: available auxiliary value, not visible in reviewed radial output
  annotation_value: empty in inspected data
  label: empty in inspected data
optional_modules:
  data_optional_csv: absent
  visible_optional_layers: outer arc labels and category text around circle
  needed_for_full_reuse: Provide explicit optional tables for labels, ordering, legends, manual positions, annotations, and
    secondary tracks when those are visible.
false_positive_risks:
- generic_renderer_rebuild
- many rotated labels require enough export size
- small radial tick labels may be hard to read
best_for:
- radial category-value summaries with grouped sectors
bad_for:
- precise value comparison across many bars
- long category labels
style_notes:
- moderate text/mark density
- Use the observed layout and scale cues, but do not infer undeclared domain semantics from placeholder CSV labels.
retrieval_tier_recommendation:
  tier: support
  rationale: Recommended below core because review found generic-renderer/fallback/synthetic or incomplete semantic-support
    risks; still useful for visual pattern retrieval at the stated tier.
  exclusion_risks:
  - generic_renderer_rebuild
confidence:
  overall: medium
  visual_grammar: high
  data_roles: medium
  code_understanding: medium
image_observation:
  image_read: true
  panel_count: single radial panel
  layout: single radial panel
  marks: *id001
  encodings: bar length encodes magnitude; hue groups category bands; radial position orders labels
  axes: polar radius with numeric ring ticks; angular category labels around the ring
  legends: no separate legend; color/group semantics carried by outer labels
  annotations: outer arc labels and category text around circle
  text_density: moderate
  visual_risks:
  - many rotated labels require enough export size
  - small radial tick labels may be hard to read
data_understanding:
  data_read: true
  observed_columns: *id002
  role_mapping:
    category: categorical bar position/order
    value: bar magnitude
    group: sector or color grouping when populated
    stack: secondary grouping when populated
    error_low: available interval lower value, not visible in reviewed radial output
    error_high: available interval upper value, not visible in reviewed radial output
    line_value: available auxiliary value, not visible in reviewed radial output
    annotation_value: empty in inspected data
    label: empty in inspected data
  derived_columns:
  - error_low
  - error_high
  false_positive_columns: []
  missing_optional_data: No data_optional.csv is present; visible annotations, manual layout, labels, legends, and secondary
    tracks may not be reproducible from data_main alone.
code_understanding:
  code_read: true
  backend: Python wrapper around retinue plotter_standard_renderer; metadata may name original R backend but plot.py does
    not call original plotting code directly
  entry_behavior: plot.py inserts retinue/tools on sys.path, imports plotter_standard_renderer.render, and calls render('bar',
    '环形条形图').
  hardcoded_aesthetics: Case title and renderer mode are hardcoded in plot.py; detailed palette/layout behavior is delegated
    to the generic renderer or preserved fallback image.
  output_behavior: Standard script is expected to write outputs/rebuilt.png; machine evidence reports the rebuilt PNG exists
    and is not blank-like.
  unsupported_features: Original plotting code behavior, full manual layout resources, and any undeclared annotation tables
    are not represented by plot.py itself.
proposed_metadata_patch: {}
proposed_dossier_patch: {}
