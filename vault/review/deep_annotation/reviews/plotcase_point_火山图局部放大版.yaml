case_id: plotcase_point_火山图局部放大版
annotation_version: deep_annotation_model_review_v1
annotation_status:
  status: complete
  level: model_assisted_deep_review
  human_review_required: false
  reason: Image, metadata, guide, standard script, CSV profile, machine evidence, card, schema, and prior review were inspected
    for this batch review.
annotator_model: gpt-5-codex_model_assisted_review
reviewed_at: '2026-05-21T00:00:00+08:00'
evidence:
  files_read:
  - AGENTS.md
  - schemas/deep_dossier_review.schema.yaml
  - vault/material/plotcase_point_火山图局部放大版/metadata.json
  - vault/material/plotcase_point_火山图局部放大版/agent_guide.md
  - vault/material/plotcase_point_火山图局部放大版/plot.py
  - vault/material/plotcase_point_火山图局部放大版/data_main.csv
  - vault/material/plotcase_point_火山图局部放大版/outputs/rebuilt.png
  - vault/evidence/machine/plotcase_point_火山图局部放大版.yaml
  - vault/cards/plotcase_point_火山图局部放大版.yaml
  - vault/review/deep_annotation/reviews/plotcase_point_火山图局部放大版.yaml
  data_optional: absent
  machine_evidence: vault/evidence/machine/plotcase_point_火山图局部放大版.yaml
  asset_card: vault/cards/plotcase_point_火山图局部放大版.yaml
  review_record: vault/review/deep_annotation/reviews/plotcase_point_火山图局部放大版.yaml
  image_size:
    exists: true
    size_bytes: 135785
    width: 1085
    height: 1011
    mean_luma: 247.0729
    stddev_luma: 36.8487
    blank_like: false
  data_rows_inspected: 29
  rebuild_class:
    source_code_rebuild: true
    generic_renderer_rebuild: true
    case_level_fallback: false
    synthetic_data: false
observed_visual_grammar:
  geometry: volcano scatter plot
  subtype: main scatter with zoomed inset
  layout: one main panel plus one inset callout
  primary_marks: &id001
  - points
  - threshold line
  - red zoom rectangle
  - dashed connectors
  - text labels
  visible_encodings: x encodes log2 fold change; y encodes -log10 p-value; color separates point classes/highlights
inferred_visual_grammar:
  declared_geometry: scatter
  declared_subtype: volcano
  metadata_keywords:
  - scatter
  - point_layer
  - continuous_axes
  reusable_pattern: volcano plots with local magnification
  domain_semantics: Only visual roles were reviewed; biological or clinical meanings are not treated as authoritative unless
    visible in labels/axes.
  core_exclusion_note: Generic renderer, fallback, and synthetic-data flags prevent promotion to core retrieval.
reviewed_visual_roles:
  marks: *id001
  encodings: x encodes log2 fold change; y encodes -log10 p-value; color separates point classes/highlights
  axes: Cartesian fold-change and p-value axes
  legends: no explicit color legend visible
  annotations: labeled points, directional arrow, zoom inset
canonical_columns: &id002
- x
- y
- group
- value
- size_value
- label
- threshold
- facet
- highlight
required_data_semantics:
  x: log2 fold-change coordinate
  y: -log10 p-value coordinate
optional_data_semantics:
  group: point color class
  value: auxiliary signed value, not directly labeled in image
  size_value: available point size field
  label: point labels
  threshold: threshold/reference value
  facet: empty facet field
  highlight: zoom/highlight selector
optional_modules:
  data_optional_csv: absent
  visible_optional_layers: labeled points, directional arrow, zoom inset
  needed_for_full_reuse: Provide explicit optional tables for labels, ordering, legends, manual positions, annotations, and
    secondary tracks when those are visible.
false_positive_risks:
- generic_renderer_rebuild
- inset selection and label placement are layout-specific
- threshold is present but p-value semantics beyond labels are inferred from axis text
best_for:
- volcano plots with local magnification
bad_for:
- unlabeled high-density volcano plots where automated labels would overlap
style_notes:
- medium text/mark density
- Use the observed layout and scale cues, but do not infer undeclared domain semantics from placeholder CSV labels.
retrieval_tier_recommendation:
  tier: support
  rationale: Recommended below core because review found generic-renderer/fallback/synthetic or incomplete semantic-support
    risks; still useful for visual pattern retrieval at the stated tier.
  exclusion_risks:
  - generic_renderer_rebuild
confidence:
  overall: medium
  visual_grammar: high
  data_roles: medium
  code_understanding: medium
image_observation:
  image_read: true
  panel_count: one main panel plus one inset callout
  layout: one main panel plus one inset callout
  marks: *id001
  encodings: x encodes log2 fold change; y encodes -log10 p-value; color separates point classes/highlights
  axes: Cartesian fold-change and p-value axes
  legends: no explicit color legend visible
  annotations: labeled points, directional arrow, zoom inset
  text_density: medium
  visual_risks:
  - inset selection and label placement are layout-specific
  - threshold is present but p-value semantics beyond labels are inferred from axis text
data_understanding:
  data_read: true
  observed_columns: *id002
  role_mapping:
    x: log2 fold-change coordinate
    y: -log10 p-value coordinate
    group: point color class
    value: auxiliary signed value, not directly labeled in image
    size_value: available point size field
    label: point labels
    threshold: threshold/reference value
    facet: empty facet field
    highlight: zoom/highlight selector
  derived_columns: []
  false_positive_columns: []
  missing_optional_data: No data_optional.csv is present; visible annotations, manual layout, labels, legends, and secondary
    tracks may not be reproducible from data_main alone.
code_understanding:
  code_read: true
  backend: Python wrapper around retinue plotter_standard_renderer; metadata may name original R backend but plot.py does
    not call original plotting code directly
  entry_behavior: plot.py inserts retinue/tools on sys.path, imports plotter_standard_renderer.render, and calls render('scatter',
    '火山图局部放大版').
  hardcoded_aesthetics: Case title and renderer mode are hardcoded in plot.py; detailed palette/layout behavior is delegated
    to the generic renderer or preserved fallback image.
  output_behavior: Standard script is expected to write outputs/rebuilt.png; machine evidence reports the rebuilt PNG exists
    and is not blank-like.
  unsupported_features: Original plotting code behavior, full manual layout resources, and any undeclared annotation tables
    are not represented by plot.py itself.
proposed_metadata_patch: {}
proposed_dossier_patch: {}
