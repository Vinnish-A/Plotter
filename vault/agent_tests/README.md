# Compatibility Path

Agent figure-generation tests now live under:

```text
bastard/tests/
bastard/generated/
```

Use:

```bash
python bastard/tests/run_agent_plot_tests.py
```

The wrapper `vault/agent_tests/run_agent_plot_tests.py` is kept only for older commands.
