# Scene Card

Every task starts as a Scene Card.

The Scene Card records the scientific intent, data types, required variables, optional variables, complexity target, defamiliarization target, output requirements, and constraints.

It is the fairness unit for comparing candidates. Candidates are judged against the same Scene Card rather than against generic beauty or rarity.
