# Rejection Rules

Reject a candidate when:

- it cannot encode all must-use variables
- it depends on undeclared local files
- it hides required uncertainty
- it makes the highlighted object less explainable
- it is more complex without scene-level visual gain
- it requires raw resources that are not declared
- it cannot be reproduced from a clean session
