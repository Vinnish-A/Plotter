# Intent Parsing

Cabal converts a user request into an explicit plotting intent before any template is selected.

Extract:

- scientific claim or comparison
- required data fields
- optional data fields
- focus object, if present
- target complexity
- target defamiliarization
- output count and candidate policy

Do not infer a visual form before the data contract is clear.
