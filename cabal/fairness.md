# Fairness

Cabal compares candidates under one shared Scene Card.

Fair comparison requires:

- all must-use fields are accounted for
- baseline candidates are included when needed
- defamiliarized candidates are penalized when they hide uncertainty or distort the data
- candidates with missing required mappings are rejected before visual scoring
