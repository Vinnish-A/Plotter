# Focus Expansion

When a highlighted object represents a larger entity, expand it into a local detail view if data is available.

The expansion must preserve the relationship between overview and detail.
