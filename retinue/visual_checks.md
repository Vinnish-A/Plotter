# Visual Checks

After rendering, check that the image exists, is non-empty, has valid dimensions, and is not trivially blank.

Label and legend checks are case-specific and should be declared in metadata when required.
