# Global Local Focus Scenario

Use `summary.csv` and `samples.csv` to create one scientific figure.

Show the global relationship between signed effect and correlation across classes. Highlight
`Class_07`. Expand `Class_07` with sample-level detail so the viewer can see whether the highlighted
global point is supported by its internal samples.

Required:

- The main panel must be the global class-level relationship.
- `Class_07` must be visually prominent.
- A subordinate detail panel must show sample-level structure for `Class_07`.
- Use p-value or FDR only as supporting context, not as the primary visual claim.
- Avoid default plotting-library color cycles.
- Export `outputs/rebuilt.png`.
- Open the image, self-check panel hierarchy and information density, and write
  `outputs/agent_self_review.json`.
